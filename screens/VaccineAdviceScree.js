import React from "react";
import {
  View,
  Text,
  TouchableOpacity,
  Button,
  Flatlist,
  StyleSheet,
} from "react-native";

const VaccineAdviceScreen = (props) => {
  return (
    <View>
      <Text>Vaccine Advice Screen </Text>
    </View>
  );
};

const styles = StyleSheet.create({
  screen: {
    flex: 1,
  },
});

export default VaccineAdviceScreen;
