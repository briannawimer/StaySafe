import React from "react";
import { View, Text } from "react-native";

const CovidScreen = (props) => {
  return (
    <View>
      <Text>You have reached the Covid Screen</Text>
    </View>
  );
};

export default CovidScreen;
