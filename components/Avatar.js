import * as React from "react";
import { Avatar } from "react-native-elements";

export default () => {
  return (
    <Avatar
      activeOpacity={0.2}
      avatarStyle={{}}
      containerStyle={{ backgroundColor: "#BDBDBD", marginRight: 10 }}
      icon={{}}
      iconStyle={{}}
      imageProps={{}}
      onPress={() => alert("Account Button")}
      overlayContainerStyle={{}}
      placeholderStyle={{}}
      rounded
      size="small"
      //source={{ uri: "" }}
      title="P"
      titleStyle={{}}
    />
  );
};
